import React from 'react';
import s from './Login.module.css'

const Login = (props) => {
  return (
    <div className={s.loginBlock}>
      <h2>Login Page</h2>
    </div>
  );
}

export default Login;